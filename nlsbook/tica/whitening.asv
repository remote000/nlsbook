function [Z,V]=whitening(X)
    in
    Z=X-ones(size(X,1),1)*mean(X);
    covm=Z*Z'/size(X,2);
    [Q,D]=eig(covm);
    [d2,order]=sort(diag(D),'descend');
    d2=d2.^-0.5;
    Q=Q(:,order);
    V=diag(d2)*Q';
    Z=V*Z;
end