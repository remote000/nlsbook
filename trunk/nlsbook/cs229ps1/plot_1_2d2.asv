load q2x.dat
load q2y.dat
tao=[0.8,0.1,0.3,2,10;
x1max=max(q2x);
x1min=min(q2x);
step=(x1max-x1min)/100.0;
x=x1min:step:x1max;
n=size(x,2);
for i=1:n
    y(i)=local_weighted_LR(x(i),q2x,q2y,tao);
end
plot(q2x,q2y,'.',x,y,'-r');
xlabel('x');
ylabel('y');